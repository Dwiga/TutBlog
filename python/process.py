class pertamax:
    def satu(self, isi):
        return isi + " + and we love python"

    def dua(self, angsat, angdu, proc):
        if proc == "+":
            a = int(angsat) + int(angdu)
            return a
        elif proc == '-':
            b = int(angsat) - int(angdu)
            return b